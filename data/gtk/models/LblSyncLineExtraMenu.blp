using Gtk 4.0;

menu timestamp_actions {
  section {
    item (_("Clear Start Timestamp"), "clear.start")
    item (_("Clear End Timestamp"), "clear.end")
    item (_("Clear All Timestamps"), "clear.all")
  }
}